The **`<optgroup>`** [HTML](https://developer.mozilla.org/en-US/docs/Web/HTML) element creates a grouping of options within a [`select`](select!) element.

> **Note:** Optgroup elements may not be nested.
